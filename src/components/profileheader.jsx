import '../index.css';

const ProfileHeader = () => {
    return (
        <header className="profile-header">
            <h1>Profile Header</h1>
        </header>
    );
}

export default ProfileHeader;